Follow this to run the Backend 
# python -m venv .venv
#.\.venv\Scripts\activate
# pip install -r requirements.txt
# pip install "uvicorn[standard]" fastapi
# python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000
